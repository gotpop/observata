{% set card_title = attributes.cardTitle|default('') %}
{% set card_text = attributes.cardText|default('') %}
{% set icon_geo = attributes.iconGeo|default('01') %}

<article class="card-geo-shader">
	{% include 'icons/geo/' ~ icon_geo ~ '.twig' %}

	<canvas class="shader" width="320" height="320" aria-hidden="true"></canvas>

	<div class="body">
		{% if card_title %}
			<h3 class="title">{{ card_title|wp_kses_post }}</h3>
		{% endif %}
		{% if card_text %}
			<p class="text">{{ card_text|wp_kses_post }}</p>
		{% endif %}
	</div>
</article>
