{% set heading = attributes.heading|default('') %}
{% set content = attributes.content|default('') %}

<article class="card-text-simple">
	<h2 class="heading-md">{{ heading|wp_kses_post }}</h2>
	<p class="body-md">{{ content|wp_kses_post }}</p>
</article>
