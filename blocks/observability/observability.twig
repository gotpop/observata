{% set section_title = attributes.sectionTitle|default('') %}
{% set intro_text = attributes.introText|default('') %}

<section class="section-block block-observability">
	<block-content class="block-content">
		{% include 'template/section-intro/section-intro.twig' with { 'attributes': { 'sectionTitle': section_title, 'introText': intro_text } } %}

		<article class="card-large">
			{% include 'graphics/tech/connected-dots-graphic-mono.twig' %}
			<layout-micro class="layout-micro">
				{{ content|raw }}
			</layout-micro>
			<card-large-content class="card-large-content">
				{% set card_title = attributes.cardTitle|default('') %}
				{% set card_body = attributes.cardBody|default('') %}
				{% if card_title %}
					<h4 class="title">{{ card_title|wp_kses_post }}</h4>
				{% endif %}
				{% if card_body %}
					<p class="body">{{ card_body|wp_kses_post }}</p>
				{% endif %}
			</card-large-content>
		</article>
	</block-content>
</section>
