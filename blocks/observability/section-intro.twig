		{% if section_title %}
			<h2 class="observability-title">{{ section_title|esc_html }}</h2>
		{% endif %}

		{% if intro_text %}
			<p class="observability-intro">{{ intro_text|esc_html }}</p>
		{% endif %}
